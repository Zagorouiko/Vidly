@model Vidly.ViewModels.Customer
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<ul>
    @foreach (var customer in Model.Customers)
    {
        //FIX iteration with Customer model instead of viewModel.
        @*<li>@Html.ActionLink(@customer.Name, "Details", "Customers", @customer.id)</li>*@
        <a href="details/@customer.id">@customer.Name</a>
    }
</ul>